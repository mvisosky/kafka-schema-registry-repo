## [1.8.0] - 2026-03-30
 🟢 **[JSON Updated]**

**feat: scaffold notification-schemas**

- This PR scaffolds the directory structure and child `pom.xml` for the new **notification-schemas** module.
- Adding notification JSON schemas

### 📦 Maven Coordinates
Add the following dependency to your `pom.xml` to consume this version from GitHub Packages:

```xml
<dependency>
    <groupId>com.example.kafka.schemas</groupId>
    <artifactId>notification-schemas</artifactId>
    <version>1.8.0</version>
</dependency>
```

