## [1.5.0] - 2026-03-29

**feat: scaffold human-resources**

This PR automatically scaffolds the directory structure, child `pom.xml`, and updates the parent `pom.xml` for the new **human-resources** domain.

Added Employee hired schema

### 📦 Maven Coordinates
Add the following dependency to your `pom.xml` to consume this version from GitHub Packages:

```xml
<dependency>
    <groupId>com.example.kafka.schemas</groupId>
    <artifactId>humanresources-schemas</artifactId>
    <version>1.5.0</version>
</dependency>
```

