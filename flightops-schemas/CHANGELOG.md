## [1.6.0] - 2026-03-30
 🔵 **[Avro Updated]**

**feat: scaffold flightops-schemas**

- This PR scaffolds the directory structure and child `pom.xml` for the new **flightops-schemas** module.
- Adding initial flightops schemas

### 📦 Maven Coordinates
Add the following dependency to your `pom.xml` to consume this version from GitHub Packages:

```xml
<dependency>
    <groupId>com.example.kafka.schemas</groupId>
    <artifactId>flightops-schemas</artifactId>
    <version>1.6.0</version>
</dependency>
```

